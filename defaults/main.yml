---
hostname_hosts_file_location: "/etc/hosts"
hostname_hosts_backup: false
hostname_fqdn_full: "{{ inventory_hostname }}"
hostname_fqdn_short: "{{ inventory_hostname_short }}"
hostname_hosts_ipv6_enabled: true
hostname_hosts_ipv4_enabled: true
